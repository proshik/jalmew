spring:
  data:
    mongodb:
      host: localhost
      username: user
      password: password
      database: admin
      port: 27017

server:
  context-path: /uaa
  port: 5000

logging:
  level:
    org.springframework.security: DEBUG