spring:
  application:
    name: ytranslate-service
  cloud:
    config:
      uri: http://config:8888
      fail-fast: true
      password: password
      username: user

---
spring:
  profiles: dev
  cloud:
    config:
      enabled: false
